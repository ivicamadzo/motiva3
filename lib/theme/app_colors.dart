import 'package:flutter/material.dart';

class AppColors {
  AppColors._();

  // Primary wellness palette
  static const Color primary = Color(0xFF7C6BFF);
  static const Color primaryLight = Color(0xFFA393FF);

  // Accent colors
  static const Color danger = Color(0xFFFF6B81);
  static const Color success = Color(0xFF4FD1C5);

  // Backgrounds
  static const Color background = Color(0xFF141129);
  static const Color surface = Color(0xFF211D3D);

  // Panic mode
  static const Color panicBackground = Color(0xFF0F172A);

  // Text
  static const Color textPrimary = Color(0xFFF8FAFC);
  static const Color textSecondary = Color(0xFFCBD5E1);
  static const Color textMuted = Color(0xFF94A3B8);

  // Borders
  static const Color border = Color(0xFF312E55);

  // Cards / Shadows
  static const Color cardShadow = Color(0x33000000);
}
